spring.application.name=platmod

# --- CONFIGURACION DE RENDER (POSTGRESQL) ---
spring.datasource.url=jdbc:postgresql://dpg-d659e5hr0fns73863mdg-a.oregon-postgres.render.com:5432/cursos_db_fg84
spring.datasource.username=cursos_db_fg84_user
spring.datasource.password=eN8sWWUkVfBR9HUPRT6iXkZQi334GotW
spring.datasource.driver-class-name=org.postgresql.Driver

# --- CONFIGURACION JPA / HIBERNATE ---
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# --- CONFIGURACION DEL SERVIDOR ---
server.port=8080
spring.jackson.serialization.fail-on-empty-beans=false

# --- GMAIL CONFIG (SSL 465 - MODO FUERTE) ---
spring.mail.host=smtp.gmail.com
spring.mail.port=465
spring.mail.username=platmod.notificaciones@gmail.com
spring.mail.password=abif cbpi groe baxs

# IMPORTANTE: Forzamos el protocolo SMTPS
spring.mail.protocol=smtps

# Configuración de seguridad (SSL SI, STARTTLS NO)
spring.mail.properties.mail.smtps.auth=true
spring.mail.properties.mail.smtps.starttls.enable=false
spring.mail.properties.mail.smtps.ssl.enable=true
spring.mail.properties.mail.smtps.ssl.trust=smtp.gmail.com

# Timeouts (Los mantenemos altos)
spring.mail.properties.mail.smtps.connectiontimeout=10000
spring.mail.properties.mail.smtps.timeout=10000
spring.mail.properties.mail.smtps.writetimeout=10000

# Debug
spring.mail.properties.mail.debug=true